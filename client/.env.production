# Used by `vite build`. An API_BASE_URL set in the Vercel project settings
# overrides this file.
#
# Production alias — stable across deploys. The per-deployment URL
# (server-<hash>-...) changes every time you push, so don't use it here.
API_BASE_URL=https://server-sidsigma3s-projects.vercel.app
