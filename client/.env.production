# Used by `vite build`. A VITE_API_BASE_URL set in the Vercel project settings
# overrides this file, so prefer setting it there once you have a stable domain.
VITE_API_BASE_URL=https://server-fvz05yehs-sidsigma3s-projects.vercel.app
