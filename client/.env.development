# Used by `npm run dev` (vite). Intentionally empty: an empty base URL keeps
# requests same-origin so the /api proxy in vite.config.js forwards them to
# http://localhost:4000. Override in `client/.env.local` if you need to point
# local dev at a deployed backend.
API_BASE_URL=
